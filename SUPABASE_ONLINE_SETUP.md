# Battle à la carte 通信対戦の設定手順

## 現在の状態

2026-09-12: 既存Web版・スマホ版にオンライン対戦の初版を組み込みました。
CPU対戦、カード・料理・イベント・加工アイテムの定義、画像パスは維持しています。
共通Supabaseの本番データベースへの追加SQL適用と、実際の2端末での通信確認はまだです。
この作業からGitHubへのアップロードや有料プランへの変更はしていません。

## 今回必要な追加設定（初版を導入済みの方）

1. 共通プロジェクト aniani-common の左メニューで SQL Editor を押します。
2. New query（新しいクエリ）を押し、supabase/battle-guest-chat.sql の内容を全部貼り付けます。
3. Run（実行）を押します。「成功。返された行はありません」は正常終了です。保存ボタンではありません。
4. 左メニューの Authentication → Sign In / Providers を開きます。
5. Anonymous Sign-Ins（匿名ログイン）の Allow anonymous sign-ins をオンにして Save を押します。
6. ゲームを更新後、2台の端末でオンライン対戦を開き、メール欄を埋めずに作成・参加を試します。

初版の battle-online.sql とホームページの 001_common.sql は再実行しません。
新規のSupabaseに設置する場合だけ、battle-online.sql → battle-guest-chat.sql の順に実行します。
追加SQLは何度実行しても既存残高や部屋を削除しません。

この追加SQLでは共通経済の aniani_private.ensure_user に「匿名ユーザーを拒否する」チェックも追加します。
元の処理を ensure_registered_user_original として保存して呼び出すため、登録済みアカウントの財布処理は維持します。
共通経済を後から新規設置した場合は、この追加SQLを再適用してください。
共通側でensure_userを今後更新する場合も匿名拒否チェックを維持してください。

ゲストは内部的にはSupabaseの匿名ユーザーです。登録メールやパスワードは不要です。
共通アカウントとは別のブラウザ保存領域を使います。ゲストのデータを登録後のアカウントへ自動移行する機能はありません。
ブラウザの保存データを消すとゲストの部屋には復帰できません。メダル等のアカウント保存には登録が必要です。
ゲーム内の既存ローカル所持数を共通財布へ移す処理と、通信対戦の報酬付与はありません。

挨拶は画面下の「挨拶」を押して定型文を選びます。待機中・対戦中・終了後に利用でき、退出後は送れません。
待機中の挨拶も参加した相手に表示します。表示は直近30件、送信はサーバー側でも1人5秒に1回です。
先攻は参加成功時にサーバーで抽選して部屋へ保存します。再接続では抽選し直しません。

匿名アカウントの大量作成への対策として、本格公開時にはCAPTCHA・Authのレート制限・不要な匿名ユーザーと古い部屋の削除運用が必要です。
公式手順: https://supabase.com/docs/guides/auth/auth-anonymous

## 1. 正しいSupabaseを開く

あにあに共通の管理画面:
https://supabase.com/dashboard/project/bjrewemniqfchggrrkdd

プロジェクト名は aniani-common です。以前作成した arfffgebpexoymnprlqk ではありません。
supabase-config.js は、あにあにホームページの common/config.js と同じ公開設定にしてあります。
URLやPublishable keyをもう一度送る必要はありません。

設定を確認する場合は上部の Connect でProject URL、Project Settings > API Keysで
Publishable key（sb_publishable_から始まる鍵）を確認できます。
secret key、service_role、データベースのパスワードをHTMLやJavaScriptに入れないでください。

## 2. 対戦用のSQLを1回だけ実行

1. 上記プロジェクトの左メニューで SQL Editor を開きます。
2. 「+」または「New query」で新しいクエリを開きます。
3. このフォルダの supabase/battle-online.sql を開き、内容全体をクエリ欄へ貼り付けます。
4. プロジェクトが aniani-common であることを確認して Run を押します。
5. 「Success. No rows returned」などの成功表示を確認します。

このSQLは新しい battle_* テーブルと balc_* 関数・権限設定だけを追加します。
既存の aniani_* テーブル、コイン残高、ガチャ設定には触れません。
ホームページ側の 001_common.sql を再実行する必要はありません。
本ファイルは初回適用用です。2回目に「already exists」と出た場合、既存テーブルを削除せず確認してください。
全体をbegin/commitで囲んでいるため、途中でエラーが出た場合はエラー内容を確認してから全体を再実行してください。

追加されるもの:

- battle_rooms: 合言葉、HOST/GUEST、ターンの担当者、状態の更新番号。
- battle_checkpoints: HOSTだけが読める試合全体の途中保存。
- battle_views: 各プレイヤーだけが読める画面用データ。
- battle_match_actions: 誰がどの更新番号で何を操作したかと処理済み記録。
- balc_private.attempts: 合言葉作成・参加の試行制限。

RLSとRealtimeの対象テーブル設定もSQLに含まれています。
battle_checkpointsはRealtime配信対象にしていません。
Realtime Presenceは部屋の参加者だけが入れるprivate channelを使い、試合内容は送信しません。
既存のRealtimeポリシーを後から追加する場合、全ユーザーを無条件許可する設定にしないでください。

## 3. 共通アカウントを使う場合のログイン（任意）

Supabase管理画面へのログインと、ゲームを遊ぶプレイヤーのログインは別です。

1. あにあにホームページのマイページでプレイヤーアカウントを作成します。
2. 確認メールが届く設定の場合はメールのリンクを開きます。
3. ゲームの「オンライン対戦（合言葉）」を開きます。
4. ログイン済み表示がなければ、同じメールアドレス・パスワードでログインします。

共通側はメール配信設定と認証済みブラウザテストが別途必要な段階です。
一般ユーザーに確認メールが届かない場合は、ホームページ側のメール配信・認証設定を先に完成させてください。
メール確認を無効化したり、新しい匿名アカウントを作ったりする必要はありません。
新規登録・パスワード再設定は共通ホームページで行い、ゲームに別の登録システムは作っていません。

同じブラウザで https://donadonaa24-cyber.github.io 配下のゲームを開く場合は、
共通プロジェクトの同じ保存設定でログイン状態を共有します。
別端末・別ブラウザ・別ドメインでは同じアカウントでログインし直します。
対戦する2人には異なるプレイヤーアカウントが必要です。

## 4. GitHub Pagesへ反映

1. 既存のBattle à la carteのGitHubリポジトリを開きます。
2. 「Add file」>「Upload files」で、変更・追加したファイルをフォルダ構成を保ってアップロードします。
3. 「Commit changes」で確定します。既にPagesが公開済みなら通常の更新と同じです。
4. 「Actions」でPagesの公開処理が成功するまで待ちます。
5. 公開されたWeb版・スマホ版を開き直します。

ゲームで読み込む追加ファイルは、supabase-config.js、battle-protocol.js、
battle-engine-worker.js、battle-chat.js、network.js、online.cssです。すべてゲームフォルダの直下に置きます。
変更ファイルはweb.html、main.js、render.js、mobile/mobile.html、mobile/main-sp.js、mobile/render-sp.jsです。
モバイルからも同じ通信・ルール用ファイルを参照するので、mobileフォルダだけのアップロードでは動きません。
HTMLの名前やホームページのURLは変更していません。
SQLと手順書は運営用です。SQLをGitHubに置くだけではデータベースに適用されません。
testsフォルダは開発確認用で、ゲームの実行には不要です。

Web版: https://donadonaa24-cyber.github.io/battle-a-la-carte--/web.html

スマホ版: https://donadonaa24-cyber.github.io/battle-a-la-carte--/mobile/mobile.html

ファイルをダブルクリックしたfile://での通信対戦は対象外です。HTTPSの公開ページ、またはlocalhostのHTTPサーバーで開いてください。

## 5. 2台での確認

1. パソコンとスマホで開きます。ゲストならログインは不要です。登録済みアカウントで試す場合は別々のアカウントを使います。
2. パソコンで「オンライン対戦（合言葉）」>「部屋を作る」を押します。
3. 表示された6桁の合言葉をスマホ側で入力し、「部屋に参加」を押します。
4. 先攻・後攻がランダムで決まります。「あなたのターン」と表示された側が操作します。
5. 料理、イベント、セット、加工アイテム、手札整理、ターン交代を確認します。
6. 操作していない側で得点・料理履歴・枚数が更新されることと、相手ターンには操作できないことを確認します。
7. 片方を一時的にオフラインにし、もう片方の切断表示を確認します。
8. 接続を戻して「接続を確認」を押します。ページを閉じた場合は再ログインし「前の部屋を再開」を押します。
9. 同じボタンを連打しても二重にカードを使わないこと、勝敗が両方に表示されることを確認します。
10. 「退出」で部屋を終了し、CPU対戦も通常どおり開始できることを確認します。

一度受け付けた操作は、同じ識別番号で再確認するため、応答が遅れたときも別の操作を連打する必要はありません。
同じアカウントで複数タブから同じ試合を操作する使い方は避けてください。
合言葉の参加期限は15分、部屋の保存期限は24時間です。退出はその部屋全体を終了します。
期限後のデータはアクセスできなくなりますが自動削除はしていません。運用時には保存期間を決めた削除処理が必要です。

## MVPの範囲と制約

### 高速化用の追加SQL（2026-09-15）

現在の共通Supabaseには適用済みです。別環境へ導入する場合は次の順番です。

1. Supabaseで対象プロジェクトを開き、左の **SQL Editor** を押します。
2. 初期SQL、ゲスト・チャットSQLを適用した後、新しいクエリに `supabase/battle-broadcast.sql` の全文を貼ります。
3. 右上の **Run** を押します。自動翻訳で「逃げろ」と表示される場合がありますが、これは「実行」です。「Save/セーブ」はクエリ文の保存で、SQL実行ではありません。
4. 警告が出る場合は内容を確認します。この更新SQLは今回のポリシーを置き換えますが、既存テーブル・試合データ・コインは削除しません。
5. エラーがないことを確認し、Web/スマホ側の更新ファイルを公開します。ゲームで「従来通信（高速化SQL未適用）」が出る場合は、接続先とSQL適用を確認します。

対戦操作はPrivate Broadcast、DBは非同期checkpoint/view/action履歴保存です。一般のクライアントに両者の非公開snapshotを配信しません。切断から戻るときは同じブラウザで「前の部屋を再開」を押します。HOSTの交代はできません。

- HOSTの専用Workerが既存のcards.js/state.js/rules.js/player.jsを読み込み、両者の操作を実行します。
- GUESTは操作要求だけを送り、HOSTの確定状態を受け取ります。
- 通常の受信データは相手手札・伏せセット・山札の中身を隠します。カードIDも名前を推測できないランダム値です。
- 既存ルールで相手の材料を選ぶ「物々交換」やセットを選ぶスキル、山札の材料を選ぶスキルなどは、その選択に必要な候補を操作中の本人へ送ります。既存効果の選択方法は変更していません。
- 通常の山札閲覧はオンラインでは枚数だけです。CPU対戦の閲覧動作は維持します。
- 各部屋の参加者以外はRLSで拒否し、ゲストは全体のcheckpointを取得できません。
- HOSTの端末には試合の全情報が必要です。HOSTの覗き見・改造を防ぐ完全な不正対策ではありません。
- HOSTの接続が失われると対戦を進められません。HOSTの交代は未実装です。
- Realtimeの切断検出には時間差があります。スマホで画面を閉じるとブラウザが処理を停止する場合があります。
- ゲームの判定更新は同期しますが、演出の再生時刻をミリ秒単位で一致させるものではありません。
- 共通コイン、達成カード、旧コインからの移行、ランキング報酬は付与しません。対戦開始で旧セーブや旧コインを移行・削除しません。
- 本格公開で共通経済へ報酬を連携するには、ルール実行・山札管理・勝敗確認を信頼できるサーバーへ移し、試合ID単位で報酬の重複付与を防止してください。
- 合言葉の試行制限はアカウントごとです。大量のアカウント作成への対策、負荷試験は別途必要です。

## 実施した検証

- tests/online.test.cjs: 実際の既存ルールを読み込む回帰テスト。料理、イベント9種、ターン、伏せ札、特殊勝利、CPU進行等。
- tests/sql.test.cjs: 一時PostgreSQL（PGlite）でSQLの適用とRLS、参加制限、二重送信、古い更新拒否を確認。Supabase本番の設定確認ではありません。
- tests/browser.test.cjs: 実際のWeb版・スマホ版とHOST WorkerをEdgeで起動し、テスト用通信を通して参加・ターン交代・切断・再読み込み復帰を確認。
- 実Supabaseの匿名2クライアントでBroadcast、料理・加工・スキル、勝敗、再戦、タブを閉じた後の再開を確認済みです。登録アカウントの最新回帰と、別々の実機スマホは未確認です。詳細は `docs/ONLINE_PERFORMANCE_RESULTS.md`。

開発者向け: node --test tests/online.test.cjs

SQLテストには @electric-sql/pglite、画面テストにはPlaywrightとEdgeが必要です。
環境が異なる場合はBALC_PGLITE_PATH、BALC_PLAYWRIGHT_PATHでパッケージ位置を指定してください。

公式資料:
https://supabase.com/docs/guides/realtime/authorization
https://supabase.com/docs/guides/realtime/postgres-changes
https://supabase.com/docs/guides/realtime/broadcast
https://supabase.com/docs/reference/javascript/auth

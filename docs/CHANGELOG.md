# 変更履歴

この文書は確認できたGit履歴、既存の `UPDATE_LOG.txt`、現在のコードに基づく要約です。詳細な2026年4月の作業記録は `UPDATE_LOG.txt` を参照してください。

## 2026-09-19

### 料理融合・捨て札表示・加工交換UI: 追加・変更・削除・テスト

- プレイヤー側のドロー表示は500ms間隔を維持し、相手側は300ms間隔へ短縮。CPU戦とオンライン戦、通常補充・スキル・カード効果に共通適用。
- 捨て札の上面に最後に捨てたカード画像を表示。通常の捨て札移動演出は維持した。
- 通常料理、緊急料理、創作料理で、使用素材を中央へ集めて料理カードへ融合し、完成カードを捨て札方向へ送る演出を追加。ルール状態、得点、実際の捨て札内容は変更していない。
- 山札・捨て札の事前確認パネルと未使用状態を削除し、クリック/タップで一覧を直接開くよう変更。
- 加工アイテム等のスポットライトへ手動の「閉じる」を追加。従来の時間経過による自動終了も維持。
- 3つの加工交換ボタンを「加工アイテム交換 0/3」の1ボタンへ統合。専用モーダルで画像、効果、各3点、未交換/交換済み、点数不足、相手ターンを表示し、確認から交換までモーダル内で完結するよう変更。
- PC版・スマホ版の機能試験、オンラインルール、Web HOST/スマホGUEST統合、スマホ4画面サイズ、画像、SQLを含む21件に成功。

### カード補充・捨て札演出: 追加・変更・テスト

- CPU戦・オンライン戦の通常補充、スキル、イベント効果によるドローを、状態処理は変えず約0.5秒間隔で1枚ずつ画面へ表示する演出を追加。
- 手札やフィールドから捨て札へ移ったカードを検出し、元位置から捨て札ボタンへ縮小しながら飛ばす演出をPC版・スマホ版へ追加。
- OSの「視差効果を減らす」が有効な場合は演出を即時完了し、操作待ちを発生させないようにした。
- 両HTMLのCSS/描画スクリプトのキャッシュ識別子を更新。
- PC版・スマホ版の500ms順次表示、効果ドロー、捨て札移動を自動ブラウザ試験へ追加。オンラインルール、画像/音源、スマホ固定盤面、Web HOST/スマホGUEST統合も回帰成功。

### 対戦画面・決着演出・フリーマッチ: 追加・変更・修正

- 自分・相手のフィールド上の加工アイテムを、相手ターンや試合終了後もクリック/タップして効果確認できるよう変更。自分のスキル詳細も相手ターンと終了後に閲覧可能にした。
- スマホ固定盤面の料理タブを左端から表示し、短い画面でも手札・セット・加工等の見出しとカードが潰れにくい最小サイズへ調整。縦スクロールなしの固定画面は維持。
- 決め手料理を3秒表示し、10点以上の一皿は専用の華やかな演出に変更。その後「試合結果を見る」で勝敗、対戦時間、双方の料理数・料理名・得点を表示し、「フィールドを確認」で最終盤面へ戻れるようにした。
- 公開版のMode BGMをコードが参照する `assets/audio/battle-mode.mp3` へ配置する構成に修正。存在しない開始/終了効果音は既存通知音へ安全に統一。
- 合言葉部屋を維持したまま、公開部屋の作成・検索・一覧参加を行うフリーマッチを追加。参加者がいない場合の説明、参加成立音、DB通知取りこぼし時のPrivate Broadcast再同期を追加。
- `supabase/battle-public-match.sql` を共通Supabaseへ適用。公開待機フラグと一覧・作成・参加RPCを追加し、既存アカウント、コイン、報酬は変更していない。
- オンライン、SQL、画像/音源、ブラウザ統合、スマホ4画面サイズの関連テストに成功。実Supabaseで匿名Web HOST・スマホGUESTの公開部屋作成、検索、参加、開始、通知音を確認。

## 2026-09-17

### カードドラッグ・オンライン待機UI: 追加・変更・テスト

- 従来のクリック操作を残し、PC/スマホ、CPU/オンライン共通で手札カードをドラッグできる `card-drag.js` を追加。
- 材料カードはドラッグ中の「セット」領域、イベントカードは「発動」領域へのドロップから、既存の確認・対象選択・通信処理を呼び出す構造にした。ルール判定は重複実装していない。
- スマホでは手札の横スクロールを妨げず、縦方向のカード移動をドラッグとして扱う。ドラッグ後の誤クリックも抑止。
- オンラインのスキル選択欄へ発動条件・効果・使用回数を追加。部屋作成/参加をどちらもオレンジ表示にし、処理中・確定後はグレー表示に変更。
- 6桁合言葉の作成・共有・入力手順を待機画面へ追加し、未入力または形式不正の参加操作には具体的な日本語メッセージを表示。
- ブラウザ統合テストでCPU戦のクリック、PCマウス/スマホタッチの材料・イベントドラッグ、オンラインHOSTの既存通信経路、スキル説明、ボタン状態、合言葉検証を確認。オンラインルール15件とスマホ固定画面4サイズの検査も成功。
- 変更対象は `card-drag.js`、両render、`network.js`、`online.css`、両HTML、ブラウザテスト、標準ドキュメント。カードルール、CPU思考、Supabase SQL、画像は変更なし。

### オンラインスマホ固定画面: 変更・修正

- 縦スクロール版を、相手/中央/自分の3段固定グリッドへ変更。通常対戦の配色と領域順は維持し、中央の山札・料理と右側の主要操作を独立配置。
- 相手の手札/HUD/セット/加工領域を優先的に圧縮し、自分の手札・キャラクター情報・加工購入ボタンを画面高に応じて調整。700px以下、600px以下の2段階で文字・画像余白を縮小。
- 料理候補一覧は盤面を押し広げない一時パネルに変更。設定画面は固定オーバーレイのまま維持。
- `tests/mobile-layout.test.cjs` を通信から分離し、画面サイズごとに独立ページを使用。相手手札6枚、双方セット3枚、加工3種、料理、自分手札5枚で領域重なり、全領域の画面内収まり、縦内容切れ、縦スクロールなし、設定画面表示を検査。
- 390×670、375×667、320×568、390×844の4サイズで表示テスト成功。Web HOST/スマホGUESTの模擬通信回帰と既存CPUルール確認も成功。
- 変更対象は `online.css`、両HTMLのCSSキャッシュ番号、表示テスト、PROJECT_SPEC、ARCHITECTURE、CURRENT_STATE、本履歴。CPU戦用CSS、ゲームルール、通信処理、画像、Supabase設定は変更なし。

## 2026-09-16

### 固定画面UI調整: 中断記録

- ユーザー要望により、オンラインスマホの縦スクロールを廃止する調整へ着手。ローカルのonline.cssで固定グリッド、相手領域縮小、カードサイズ追従、料理候補ポップアップを試作。
- mobile-layoutテストに全領域の画面内収まり・縦スクロール禁止の検査を追加。設定ポップアップ表示で失敗しており、修正完了とはしていない。
- 盤面の列幅と上部余白を追加修正した直後、ユーザーのPC終了で中断。変更はローカル保存のみで、GitHubへ未反映。再開情報はGit対象外のdocs/ACTIVE_TASKS.mdに記録。

### 接続互換性・スマホ配置: 修正・変更

- 従来の一律エラーはSQL不足以外でも表示されることを確認。設定、回線、タイムアウト、認証制限、SQL/RLS、SDK、ブラウザ互換性を日本語で区別し、ログは診断コードのみとした。遅れて完了したログイン確認がエラー文を上書きする問題も修正。
- 古いブラウザで未対応のAbortSignal.timeout、crypto.randomUUID、Object.hasOwnへの依存を解消。UUIDは暗号学的乱数を維持。主SDK CDNの読込失敗時は同じバージョンの代替CDNへ切替。
- 通常対戦の参考画像に合わせ、オンラインスマホ画面の配色・領域順を維持。絶対配置された中央領域が双方のセット/加工領域へ重なる問題を通常フローで解消し、短い画面は縦スクロールに変更。
- 対戦中の通信パネルは「通信」で開閉し、待機/切断/終了時は自動表示。設定画面はスクロール位置に関係なく表示。
- network/protocol/worker/chat、online.css、両HTML、関連テスト、CURRENT_STATE/PROJECT_SPEC/ARCHITECTUREと本履歴を変更。修正前はローカルZIPへバックアップ。CPUルール、CPU用CSS、画像原本、SQL、コイン、認証設定は変更なし。

### テスト・調査

- 関連17件成功。主CDNを遮断し、旧ブラウザ相当のAPI欠落を模擬したWeb/スマホ画面でSDK代替読込、部屋作成/参加、選択/ターン、切断/復元を確認。既存CPU進行・料理・イベント・加工・勝敗テストも成功。
- 表示fixtureで390×670、375×667、320×568、390×844のセット/加工/料理/候補領域の重なり検査に成功。
- 公開先のHTML/通信ファイルが200で取得でき、現在のSupabaseは匿名で部屋作成/参加可能であることを確認。ユーザー実機の一律エラーの厳密な発生原因は未再現で、SQL未設定とは断定しない。
- 実通信計測で高速連打時に中間Viewが新しい状態に追い越され、期待件数に達しない試行があったため、計測操作ごとに相手DOM到達を待つようテストを調整。
- 調整後の実Supabase試験1件成功。Web HOST/スマホ版GUESTでセット、料理、加工、双方の異なるスキル、勝敗、再戦、GUEST新タブ再開、HOST再読込を確認。関連テストと合わせ18件成功。
- 実測GUEST40回平均84.18ms/中央値83.10ms/82.30〜97.60ms、HOST20回平均92.19ms/中央値86.65ms/84.20〜122.60ms。初期画像はWeb18件860,170bytes、スマホ15件706,550bytes、原本PNG読込0件（当該試行の選択キャラ/手札に依存）。実機Android/iPhoneは未確認。

## 2026-09-15

### オンライン選択修正: 原因確認・変更・修正

- 選択欄が「メニューへ戻る」より下にあり、部屋作成/参加後はロックされる導線を確認。選択欄を部屋操作より上へ移動し、確定タイミングと変更方法を明記。
- 再接続時に試合状態は復元される一方、選択欄はプロフィール初期値のままだった表示不一致を修正。DBの本人 `host_info/guest_info` から欄を復元。
- 認証/部屋RPCの前に選択結果を固定。キャラ/スキルIDの検証、不正なプロフィール初期値の安全な代替、APIからの明示的なスキル指定を追加。
- 選択結果は `balc_create_room/balc_join_room` の `p_info` から参加者別に保存され、同じWorker/ルールと `me/opponent` 描画を使用することを確認。HOST/GUESTのスキル誤適用は今回の試験では再現なし。
- 相手スキルの対戦画面「？？？」表示は従来仕様として維持。CPUキャラ抽選・CPUスキル処理・ゲームルール・SQL/Broadcast構成は変更なし。
- `network.js`, `web.html`, `mobile/mobile.html`, 関連3テスト、`docs/CURRENT_STATE.md`, 本履歴を変更。HTMLのnetworkキャッシュ識別子を更新。修正前ファイルはローカルZIPへバックアップ。
- 仕様・通信構成自体の変更はないため、PROJECT_SPEC/ARCHITECTUREは変更なし。

### オンライン選択修正: テスト

- online/browser関連15件成功。全4キャラ×6スキルの参加者別適用・View復元、Web版同士の別キャラ/別スキルのRPC保存と双方View、GUESTの選択欄復元、既存CPU進行・イベント・勝敗を確認。
- 実Supabase試験成功。HOST「拓海/食材トラップ」、GUEST「暁/味見泥棒」で合法スキル使用、本人/相手Viewの使用回数、カードセット、料理、加工、ターン、勝敗、双方再戦、再戦時リセット、GUEST新タブ再開、HOST再読込再開を確認。
- 最終実測はGUEST40回平均84.94ms/中央値84.45ms/83.10〜92.60ms、HOST20回平均104.29ms/中央値89.70ms/86.50〜148.10ms。確認系入力→相手DOMの値です。GUEST入力のBroadcast到達区間は平均83.32ms、Worker約0.72ms、相手DOM更新約0.32msで通信が主な待ち時間。
- 実機Android/iPhone上の報告現象そのものは未再現。PC Edgeの独立2コンテキスト、Web版/スマホ版（スマホ幅）で検証。物理断線・端末固有ブラウザは引き続き未確認。

### 高速化実装: 追加・変更・修正

- `network.js` に宛先別Private Broadcast、即時UI予測、HOST Worker処理、非同期checkpoint保存・再送・復帰を追加。表示からDB保存待ちと操作ごとのREST再取得を除去。
- `battle-view-model.js` に共通 `me/opponent` 変換を追加。Web/スマホ描画を変換モデルへ移し、オンラインCPU見出し・ログ・CPU設定表示を整理。
- オンラインキャラ/スキル選択、双方承認の同室再戦、ランダム先攻と切断後の再開を整備。
- `battle-metrics.js` に地点別性能計測、`battle-images.js` に軽量/原本パス変換を追加。
- 対戦用WebP65枚と生成ツール/manifestを追加。旧56枚と同じ集合を141.93MiBから2.98MiBへ削減。全件初期プリロードを廃止し、デコード済み画像を保持。
- ギャラリー原本PNGを維持。既存ルール、CPU処理、原本画像は変更・削除なし。
- 修正前の対象ファイルをローカルZIPへバックアップ。バックアップはGit対象外。
- 標準文書・Supabase手順を更新し、`docs/ONLINE_PERFORMANCE_RESULTS.md` に詳細結果・制約を追加。

### 高速化実装: 設定変更

- 承認を受け共通Supabaseへ `supabase/battle-broadcast.sql` を適用。参加者限定Broadcast権限、HOST限定checkpoint保存・再戦RPCを追加。既存データ削除、コイン/アカウント設定変更なし。
- 変更したHTML/JS/CSSのキャッシュ識別子を更新。

### 高速化実装: テスト

- online/sql/browser/images回帰16件成功。checkpoint保存失敗→再送→新状態保存も成功。
- 実Supabase試験1件成功。PC幅/スマホ幅で部屋作成、参加、キャラ/スキル選択、セット、料理、加工、スキル、ターン、勝敗、再戦、実タブ切断/再開を確認。
- HOST40回: 平均93.25ms、中央値86.35ms、84.80〜143.10ms。GUEST20回: 平均84.19ms、中央値84.15ms、83.00〜85.60ms。入力から相手DOM完了まで、60回加重平均90.23ms。
- 起動〜対戦開始の画像取得は今回Web635,814 bytes、スマホ幅194,614 bytes、原本PNG取得0。
- 実機Android/iPhone・物理断線・全ストーリー手動回帰は未確認。詳細と測定範囲は結果文書を参照。

### 追加

- `docs/ONLINE_PERFORMANCE_AUDIT.md` にSupabase通信、HOST/GUEST表示、画像容量の調査結果を追加。

### 調査

- 使用中SupabaseがAWSソウル (`ap-northeast-2`) であることを確認。
- 対戦操作がBroadcastではなくRPC、Postgres Changes、REST再取得を通ることを確認。
- 公開版の軽量操作8回で平均約1,010msの遅延を再現。
- Worker処理が中央値0.203msで、通信の直列往復が主因であることを確認。
- 対戦プリロード56画像が141.93MiB、RGBA展開相当で約335.99MiBであることを確認。

### 変更

- `docs/CURRENT_STATE.md` に調査結果と次の作業候補を反映。

### テスト

- ゲームコード、SQL、画像は変更せず、公開版2クライアントと読み取り中心の診断で確認。

## 2026-09-14

### 追加

- `AGENTS.md` と `docs/` 配下のプロジェクト継続用ドキュメント一式を追加。
- 標準5文書をソースコードと一緒にGit管理する共通ルールを追加。
- `docs/ACTIVE_TASKS.md` をローカル専用とする `.gitignore` 規則を追加。
- 匿名ゲストによるオンライン対戦を追加。
- 待機中・対戦中・対戦後に使える6種類の定型チャットを追加。
- チャットへサーバー側5秒クールダウンを追加。

### 変更

- GUEST参加時に先攻/後攻をサーバー側でランダム決定する方式へ変更。
- Private Realtime Channelへ参加する直前に認証を更新するよう変更。
- 共通アカウントを任意とし、未登録でも部屋作成・参加できるよう変更。
- Codexの開始時確認を `AGENTS.md` と `CURRENT_STATE.md` 優先とし、無関係な全体再解析を避ける運用へ変更。
- 文書のパス表記をプロジェクトルートからの相対パスへ統一する規則を追加。

### セキュリティ

- APIキー、トークン、パスワード、秘密鍵、Secret、PC固有情報を文書やGitへ追加しない規則を明文化。

### 設定変更

- SupabaseでAnonymous Sign-Insを有効化。
- `supabase/battle-guest-chat.sql` に先攻決定、匿名対応、チャット、RLS、Realtime設定を追加。

### テスト

- オンライン/SQLテスト12件成功。
- ブラウザ統合テスト1件成功。
- 公開版で匿名2クライアントのルーム参加、対戦開始、定型チャットを確認。
- 文書5ファイルの必須見出しと参照先を検証。

## 2026-09-13

### 追加

- Supabaseによる合言葉オンライン対戦MVPを追加。
- `supabase-config.js`, `network.js`, `battle-protocol.js`, `battle-engine-worker.js`, `online.css` を追加。
- RLS、RPC、Realtime、対戦テーブルを作成する `supabase/battle-online.sql` を追加。
- 初心者向け設定手順 `SUPABASE_ONLINE_SETUP.md` を追加。

### 変更

- 既存CPUルールをWeb Workerから共通利用し、HOST管理のオンライン状態同期へ対応。
- 相手の手札、イベント、伏せセットを表示用状態から隠す処理を追加。

### テスト

- CPU対戦を残したまま、オンライン通信モジュールとSQLの自動テストを実施。

## 2026-09-07

### 追加

- 公式ホームページへGoogle Search Consoleのサイト確認メタタグを追加。

### 変更

- ホームページ、PC版、スマホ版が別URLで共存するHTML構成へ整理。

## 2026-04-29

### 追加

- `battle-a-la-carte-2/` に火力ゲージ、オーダー、コースコンボ、逆転トリガー、追加カード、第4話を含む別バージョンを追加。

### 変更

- Battle 2版の対戦画面を全画面固定レイアウトへ変更。

## 2026-04-21

### 追加

- 当時の別構成に「あにあにの遊び場」、コメント/記事、SNS導線、ミニゲームを追加した記録あり。
- 神経衰弱とローカルランキングを追加した記録あり。
- 独立公開用 `aniani-standalone` を作成した記録あり。

### 注記

- 上記 `aniani/` と `aniani-standalone/` の一部は現在の作業ルートには存在しません。公開用Git履歴では `aniani/` の存在を確認していますが、現在内容は未確認です。

## 2026-04-20

### 追加・変更

- 公式ホームページの構成、リンク、制作者コメント、画像表示を調整した記録あり。
- 詳細は `UPDATE_LOG.txt` を参照。

## 2026-04-19

### 追加・変更

- BGM切替、音量、タイトル/ストーリー/結果/Mode用BGM、表示レイアウトを調整した記録あり。
- Web版とスマホ版の共存、ファイル名・参照パス、キャッシュ識別子を調整した記録あり。

## 2026-04-18

### 追加・変更・修正

- ストーリーチュートリアル、Mode演出、キャラクター/イベント画像、スマホUI、ルール表示を多数更新した記録あり。
- ブラウザ戻る対策とCPU戦オートセーブ/再開を追加した記録あり。

## 2026-04-17

### 追加・変更・修正

- Battle a la carte Mode、カットイン、キャラクター追加、料理/イベント表示を更新した記録あり。

## 2026-04-16

### 追加・変更

- ギャラリー、ユーザー情報、ローカルコイン、戦績、背景交換を追加・調整した記録あり。

## 2026-04-15

### 追加・変更・修正

- CPU性格、スキル、背景/BGM設定、スマホ操作性、各種対戦UIを更新した記録あり。

## 2026-04-14

### 追加・変更・修正

- コイン交換所とユーザー情報を分離。
- 推しキャラ、推しスキル、最近作った料理をプロフィールへ追加。
- 対戦開始手順、スキル詳細、文字化け、HTML構造を修正。
- `UPDATE_LOG.txt` を日本語中心の記録へ整理。

## 2026-04-13

### 追加・変更・修正

- 6種類のスキルとCPU連携を試作。
- 対戦開始UIをキャラ、スキル、先攻後攻の段階式へ整理。
- Web版/スマホ版HTMLの文字コードと表示崩れを修正。

## 2026-04-12

### 追加・変更・修正

- 現行ルール、CPU対戦、料理、イベント、加工、各種UIを継続調整した記録あり。
- 個々の変更は `UPDATE_LOG.txt` を参照。

## 日付不明

### 追加

- Battle a la carte本編の初期実装。

### 注記

- 初期作成日を確定できるGit履歴が現在の公開用チェックアウトにないため、日付は推測していません。

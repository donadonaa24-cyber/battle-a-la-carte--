# 現在の開発状態

更新日: 2026-09-16

## バージョン

- 正式なセマンティックバージョン: 未定義
- GitHub公開用ブランチ: `main`
- 最後にゲーム動作を確認した実装: 2026-09-16 接続互換性・オンラインスマホ配置修正版（コミットIDはGit履歴参照）
- HTMLのキャッシュ識別子はファイルごとに異なり、製品バージョンではありません。

## 技術状態

- Unityバージョン: 該当なし。このフォルダはUnityプロジェクトではありません。
- 実装: HTML/CSS/Vanilla JavaScriptの静的サイト
- 外部SDK: Supabase JavaScript SDK 2.57.4（ESM CDN、読込失敗時は同版のUMD CDNへ切替）
- 公開: GitHub Pages
- Git管理: GitHubリポジトリの `main`。標準5文書もソースと一緒に管理
- 一時作業調整: `docs/ACTIVE_TASKS.md` は `.gitignore` 対象

## 実装済み

- 公式ホームページからPC版とスマホ版へ個別に移動
- PC向けWeb UIとスマホ向け専用UI
- CPU対戦、10点勝利、2種の特殊勝利
- 材料、イベント、料理、加工アイテム、6スキル
- 4キャラクター、表情、スキル/Modeカットイン
- Battle a la carte Mode
- 全3話のストーリーチュートリアル
- ローカルプロフィール、戦績、ローカルコイン、背景交換
- CPU戦の途中オートセーブと再開
- 対戦中のブラウザ戻る操作に対する放棄確認
- Supabase合言葉オンライン対戦
- 登録不要の匿名ゲスト対戦と任意の共通アカウントログイン
- ランダム先攻、相手非公開情報のマスク、ターン操作制限
- 定型チャット、5秒クールダウン、切断表示、再接続
- Private Broadcastによる即時操作・宛先別View、非同期checkpoint/action履歴保存
- 共通 `me/opponent` 描画モデル、オンライン時のCPU表記・CPU設定非表示
- 対戦専用WebP65枚、必要画像の優先読込、ギャラリー原本PNG維持
- オンライン専用キャラ/スキル選択、双方承認の同室再戦
- オンライン選択欄を部屋操作より上へ配置。作成/参加で確定する案内、参加直前の値固定、入力検証、再接続時の選択欄復元
- 古いブラウザ向けの通信タイムアウト/安全なUUID互換処理、SDK CDN代替読込、原因別の日本語接続エラー
- スマホオンラインの独立したセット/加工/料理領域、高さ不足時の縦スクロール、対戦中に収納可能な通信パネル

## 作業途中・未実装

- 共通「あにあにコイン」への移行とクラウド財布連携
- 通信対戦のコイン/カード/ランキング報酬
- ガチャ、ナンバー付きコレクション、ゲーム解放、デイリー要素
- HOSTに依存しないサーバー権威型対戦
- HOST離脱時の別端末へのHOST移譲
- Unity版、Android/iOSネイティブ版

## 既知の不具合・制約

- 公開用Gitでは `battle-mode.mp3` がルートにあり、コードは `assets/audio/battle-mode.mp3` を参照します。GitHub Pages上でMode BGMが404になる配置不一致が残っています。
- オンライン対戦の正規状態計算はHOSTブラウザに依存します。MVPの非公開表示は実装済みですが、改造HOSTへの完全な不正防止ではありません。
- オンライン中のローカルオートセーブは無効です。再開はSupabase上の有効な部屋とHOST接続に依存します。
- Web版とスマホ版には重複コードがあり、片側だけ変更すると差分が生じる可能性があります。
- `firebase-*`, `friend-battle-firebase.js`, `firestore.rules` は旧方式の残存ファイルで、現在のHTMLからは読み込まれていません。
- 設定全体の永続化は未確認です。
- Chrome、Safari、Firefox、実機スマホ全組み合わせの最新回帰結果は未確認です。
- GUESTの非公開情報を伴う最終結果はHOST判定の往復を待ちます。ローカル予測対象は確認・選択・確定セット等だけです。
- ブラウザオフライン模擬は既存WebSocketを閉じない場合があります。実タブ終了の切断/再開は確認済み。物理断線や画面休止はPresence心拍待ちが残ります。
- 対戦履歴は既存action履歴です。再戦単位の独立した結果アーカイブ・ランキングは未実装です。

## 外部サービス

- Supabase: `aniani-common` 接続情報を `supabase-config.js` に設定済み。
- Supabase SQL: 初期対戦SQLと匿名/チャット追加SQLは適用済みとして動作確認済み。
- Anonymous Sign-Ins: 有効化済みとして匿名2クライアント試験に成功。
- CAPTCHA/ボット対策: 未確認。
- Firebase: 現在未使用。設定手順と旧コードのみ残存。
- Unity MCP: このプロジェクトでは未設定・未使用。
- 認証情報: 新規文書には値を記載していません。既存のブラウザ用Supabase Publishable keyは公開クライアント設定としてコードに存在しますが、Secretではありません。
- Supabaseリージョン: 現在使用中の `aniani-common` はAWSソウル (`ap-northeast-2`)。詳細は `docs/ONLINE_PERFORMANCE_AUDIT.md`。
- `supabase/battle-broadcast.sql`: 2026-09-15に承認を受け適用済み。共通コイン・アカウントテーブルは変更なし。

## 通過しているテスト

最終確認日: 2026-09-16

- 2026-09-16の関連テスト（online/browser/mobile-layout）17件成功。古いAPI非搭載・主CDN失敗時の代替読込・SQL不足の表示・既存CPU進行を確認。
- 同日、390×670、375×667、320×568、390×844で双方のセット3枚、加工3種、料理、料理候補を入れた領域重なり検査に成功。設定画面もスクロール後に開けます。
- 同日の実Supabase試験成功。Web HOST対スマホ版GUESTでセット/料理/加工、異なるスキル、勝敗/再戦、GUEST新タブ再開、HOST再読込再開を確認。GUEST40回平均84.18ms/中央値83.10ms、HOST20回平均92.19ms/中央値86.65ms（確認系入力→相手DOM）。Android/iPhone実機は未確認。

- ローカル回帰（online/sql/browser/images）: 16件成功。初回checkpoint保存失敗→再送→新状態保存も確認
- 実接続の入力→相手DOM: HOST40回平均93.25ms/中央値86.35ms、GUEST20回平均84.19ms/中央値84.15ms。60回加重平均90.23ms
- 実Supabase（live-online）: 匿名Web対スマホ幅で料理・加工・スキル・勝敗・再戦・タブ切断/新タブ再開に成功
- 選択修正の関連テスト（online/browser）15件成功。全4キャラ×6スキルとWeb版同士のHOST/GUESTの異なる選択の保存・相手View・復元を確認
- 実SupabaseでHOST「拓海/食材トラップ」、GUEST「暁/味見泥棒」の別スキル使用回数を双方から確認。勝敗・再戦時リセット・GUEST新タブ再開・HOST再読込再開に成功
- 選択修正後の実測: GUEST40回平均84.94ms/中央値84.45ms、HOST20回平均104.29ms/中央値89.70ms（入力→相手DOM、確認系操作）
- 公開URLで匿名ゲスト2クライアントが同じ部屋へ参加し、ランダム先攻で対戦開始: 成功
- 同じ部屋で定型チャット「こんにちは！」のRealtime受信: 成功
- Realtime Private Channel参加前の認証更新: 成功
- 変更前の公開版軽量操作8回: 平均約1,010ms。変更後の実測は `docs/ONLINE_PERFORMANCE_RESULTS.md`
- 旧56画像と同じ集合: 141.93MiB→2.98MiB、RGBA展開相当335.99MiB→39.34MiB。全件初期プリロードは廃止

## 次に行うべき作業

1. 実機Android/iPhoneとPCで公開版を確認し、物理断線・画面休止・長時間プレイを検証する。
2. `battle-mode.mp3` を公開用の `assets/audio/` へ合わせ、Mode BGMの404を修正する。
3. PC実機とスマホ実機で、1試合を勝敗まで通すオンライン回帰試験を行う。
4. Web対スマホ、スマホ対スマホでイベント、加工、スキル、特殊勝利、切断復帰を確認する。
5. 共通コイン連携を始める前に、サーバー側の勝敗検証と重複報酬防止方式を決める。

## 最後に正常動作を確認した状態

- 更新したHTMLをHTTPサーバーで提供し、実SupabaseとPC Edgeの2独立コンテキスト（PC幅/スマホ幅）を使用。
- 両者ともメール登録なしの匿名ユーザー。
- HOSTが部屋を作成し、GUESTが6桁コードで参加。
- HOST/GUEST表示、先攻/後攻、ターン表示、Realtime接続、定型チャットを確認。
- 合法操作のみの自動試合で料理・加工・スキル・勝敗・再戦・実WebSocket切断と再開を確認。全ルールの実機スマホ完走は未確認です。
- 異なるキャラ・スキルがプレイヤーごとに保持され、スキル使用回数も相手Viewへ同期。再戦後に回数をリセットし、HOST/GUEST再開後に選択欄もDBの本人情報へ復元することを確認。
- 実機で報告された「選択できない」現象そのものは未再現。選択欄の低い配置・部屋操作後のロック・復帰時の欄と試合状態の不一致を修正。対戦中の相手スキル「？？？」は従来仕様として維持しています。
- 2026-09-16の一律接続エラーは実機固有の原因を未再現。旧ブラウザAPI依存とSDK読込経路を改善し、原因別表示へ変更。既存SupabaseのSQL/匿名接続は実接続で利用可能と確認し、外部設定は変更していません。

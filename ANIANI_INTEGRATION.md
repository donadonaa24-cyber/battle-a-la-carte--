# あにあに共通基盤との統合方針

2026-09-12: ホームページ側のローカルコードと作業記録を読み取り確認した時点の記録。
通信対戦や共通コインのゲーム組み込みが完了したという意味ではありません。

## 接続先と担当範囲

- 共通プロジェクト: aniani-common / bjrewemniqfchggrrkdd。
- supabase-config.js はホームページ側の common/config.js と同じ公開URL・Publishable keyに変更済み。
- 以前のゲーム専用プロジェクト arfffgebpexoymnprlqk は今後の接続先にしない。削除やデータ移行は実施していない。
- ホームページ側がアカウント、共通財布、デイリー、ガチャ、図鑑を担当する。
- この作業フォルダでは既存Web版・スマホ版の通信対戦と共通基盤への接続を担当する。
- ホームページ側のファイルや稼働DBは今回変更していない。既存の初期SQLは再実行しない。
- game_id は battle_a_la_carte とする。

## 既存の共通APIを再利用

共通側の実際のテーブル名は aniani_profiles、aniani_wallets、aniani_ledger、
aniani_cards、aniani_inventory、aniani_daily_claims、aniani_draws、
aniani_achievements、aniani_achievement_claims、aniani_rules。
例示された wallets 等を別途作って二重管理しない。

既存RPC: aniani_bootstrap、aniani_claim_daily、aniani_draw。
aniani_award_achievement は信頼されたサーバー処理用で、ブラウザから呼び出さない。
ブラウザに秘密鍵を配置しない。

通信対戦用の追加テーブル・RLS・RPCは、共通側の初期SQLとは別の追加SQLで用意する。
部屋の参加者は共通Supabase AuthのユーザーIDで識別し、部屋ごとの認可を行う。
通信メッセージ内の自己申告ユーザーIDや勝敗だけを信用しない。

## ログイン

共通側はメールによるプレイヤー登録・ログインを実装している。
通信対戦の未登録ユーザーには専用storageKeyで匿名ユーザーを作成する。共通アカウントのセッションは上書きしない。ゲストから共通経済RPCを利用できないよう、battle-guest-chat.sqlでensure_userに登録済みチェックを追加する。Supabase管理画面へのログインはプレイヤーログインとは別。
CPU対戦は従来どおり未ログインでも動作させる。

同じSupabaseプロジェクト・ブラウザ保存設定・公開元（オリジン）であれば
ログイン状態を共有する構成を取れる。現在の公開URLはどちらも
https://donadonaa24-cyber.github.io 配下なので、パスの違いだけでは保存領域は分かれない。
異なるドメインや端末では同じアカウントを使えても、別途ログインが必要。
トークンをURLに付けて受け渡さない。

共通側記録ではプレイヤー登録からの認証済みブラウザ試験と一般ユーザー向けメール配信設定が未完了。
ホームページの公開反映も今回この作業からは確認していない。

## コインとゲーム報酬

profile.js の現行コインは localStorage の値。表示名を変えるだけで共通財布として扱わない。
既存の残高、解放済み背景、セーブは保持する。
改変可能な旧残高を共通財布へ自動合算しない。移行特典が必要なら別途サーバー側の上限付き施策として決定する。

共通残高の表示はDBを参照し、消費はサーバー側で残高・解放条件を検証して履歴と一緒に確定する。
獲得報酬はユーザーID・game_id・試合ID・報酬種別などで重複付与を防ぐ。
HOSTのブラウザが申告した勝利やCPU戦クリアだけでは、共通コインや達成カードを付与しない。
不正防止された報酬を有効にするには、サーバー側で検証可能な試合・達成記録が必要。

対戦そのもののHOST管理MVPと、共通経済に影響する報酬の有効化は分けて検証する。
対戦の10点先取や加工アイテムに使う試合内得点は、共通コインと混同・変更しない。

## 実装順序と未完了事項

1. ホームページ側の共通ログインと財布の認証済み動作を確認する。
2. Web版・スマホ版で同じログイン状態と共通残高を読み取る接続を追加する。
3. 共通プロジェクトへ対戦用の追加SQLを適用し、通信処理を network.js 等に分離して実装する。
4. Firebase方式の読み込みを切り替え、CPU対戦・既存ルールを回帰検証する。
5. Web対Web、Web対スマホ、スマホ対スマホで参加・操作・切断・再接続を検証する。
6. 信頼できる結果検証と重複防止の試験後、共通コイン・達成カード報酬を有効化する。

2026-09-12 更新: 通信対戦のローカル初版を実装済み。HTMLの通信処理はFirebaseからSupabaseへ切り替えた。
共通財布の読み書き・報酬連携は未実装。対戦本体は共通ログインを使い、報酬を出さずに独立して検証できる。
追加SQLの本番適用と実端末間の通信確認は未実施。手順と制約は SUPABASE_ONLINE_SETUP.md を参照。
